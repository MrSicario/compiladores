NAME: Order declaration
DESCRIPTION: Functions are on a global environment, so declaration order doesn't matter
SRC:
(
    (def (f x y) (if (gt x y) x y))
    (def (gt x y) (not (<= x y)))
    (def (add2 x) (+ x 2))
    (add2 (f (add2 2) 2))
)
EXPECTED:
6