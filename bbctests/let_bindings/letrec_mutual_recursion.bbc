NAME: letrec mutual recursion
DESCRIPTION: letrec binding of two mutually recursive functions
SRC:
(letrec
	((even (lambda (n) (if (<= n 0) true (@ (print odd) (+ n -1)))))
	(odd  (lambda (n) (if (<= n 1) true (@ (print even) (+ n -1))))))
	(@ (print odd) 13))
EXPECTED:
true